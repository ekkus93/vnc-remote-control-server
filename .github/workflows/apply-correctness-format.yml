name: Apply correctness formatting pass

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/apply-correctness-format.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - name: Format complete correctness batch
        run: |
          cargo fmt --all
          git diff --check
      - name: Commit and remove workflow
        shell: bash
        run: |
          rm .github/workflows/apply-correctness-format.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "Format correctness review implementation"
          git push origin HEAD:master
